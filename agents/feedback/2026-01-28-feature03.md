# Feature 03 decisions (2026-01-28)

## Decisions
- Step‑up MFA defined as "MFA verified within reauthMaxHours" using `mfa_factors.last_used_at` (no new env).
- DB-backed job queue (table `jobs`) with `runAfter` scheduling; worker `runNextJob()` processes one job at a time.
- GDPR export ZIP built with `yazl` (minimal dependency) and uploaded to S3; exports expire after 24h.
- S3 integration uses AWS SDK v3 + `forcePathStyle` for OVH; tests use in‑memory stub.
- `consents` table structure replaced to user‑scoped versioned consents as per Feature 03 spec.
