# 2026-01-28 Feature04 Frontend Feedback

RESOLU: resend email verification endpoint implemented.
- POST /auth/email/resend (neutral response, rate limit) + audit log `EMAIL_VERIFICATION_RESEND`.

RESOLU: billing portal alias added.
- POST /billing/portal-session now routes to same handler as /billing/portal.

RESOLU: OAuth callback now redirects to `/oauth/callback?next=...` for routing.
